/*
import React, {useContext} from 'react';
import {ControlsContext} from '../context/RecordingContext';
import styles from './RecordingPage.module.scss';
import {MicOnIcon} from './icons/MicOnIcon.jsx';
import {MicOffIcon} from './icons/MicOffIcon.jsx';

export const MuteMicrophoneButton = () => {
    const {isRecording, isMicMuted, handleToggleMicMute} = useContext(ControlsContext);

    if (!isRecording) {
        return null; // Only show the button when recording is active
    }

    return (
        <div>
            Hier komt de microsofoon status
        </div>
    );
};*/